{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm --prefix client run build"
  },
  "deploy": {
    "startCommand": "npm --prefix client run preview -- --port $PORT"
  }
}
